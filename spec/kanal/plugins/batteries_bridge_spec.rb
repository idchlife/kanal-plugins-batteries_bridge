# frozen_string_literal: true

RSpec.describe Kanal::Plugins::BatteriesBridge do
  it "has a version number" do
    expect(Kanal::Plugins::BatteriesBridge::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
